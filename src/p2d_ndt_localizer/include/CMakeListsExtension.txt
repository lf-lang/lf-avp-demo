unset(BUILD_TESTING)

set(AUTOWARE_HOME /home/$ENV{USER}/adehome/AutowareAuto)

find_package(ament_cmake_auto REQUIRED)
ament_auto_find_build_dependencies()
find_package(ndt_nodes REQUIRED)

include_directories(${AUTOWARE_HOME}/src/localization/ndt_nodes/include/ndt_nodes)

ament_target_dependencies( ${LF_MAIN_TARGET} ndt_nodes)